import React, { useState } from "react";

const Menu = () => {
    const [state, setState] = useState(0);
    return (
        <div>
            <span>TODO Menu</span>
        </div>
    )
}
export default Menu;